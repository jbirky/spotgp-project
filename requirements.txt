# spotgp with JAX support. For local development use an editable install
# instead: pip install -e /path/to/spotgp[jax]
spotgp[jax] @ git+https://github.com/jbirky/spotgp.git

# Fitting and I/O
dynesty
h5py
lightkurve
pandas
pyyaml

# Pipeline
dvc

# Interactive explorer app
plotly
streamlit
sympy

# Documentation
mkdocs-material

# Optional NUTS sampler
# blackjax

# Experiment tracking (W&B used by run_fit.py + the GUI results table)
wandb

# Optional experiment tracking
# mlflow
